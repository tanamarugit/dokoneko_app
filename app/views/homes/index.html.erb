<h1>投稿一覧</h1>

<div><%= link_to "写真投稿", new_post_path %></div>

<%= current_user.posts.each do |post| %>
  <div>
    <p><%= post.caption %></p>
    <%= image_tag post.image.first %>
  </div>
<% end %>